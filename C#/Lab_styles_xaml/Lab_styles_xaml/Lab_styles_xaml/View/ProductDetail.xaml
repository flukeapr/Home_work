<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Lab_styles_xaml.ViewModels"
             x:Class="Lab_styles_xaml.View.ProductDetail">

    <ContentPage.Content>
        <StackLayout BackgroundColor="Wheat">
            <Label Text="{Binding selectProduct.Title}"
            FontSize="Large" FontAttributes="Bold"
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding selectProduct.Description}"
               FontSize="Large" 
                HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding selectProduct.Price}"
                FontSize="Large" TextColor="Red"
                HorizontalOptions="CenterAndExpand" />

            <Image Source="{Binding selectProduct.Image}" HeightRequest="200" WidthRequest="200"/>
            <Button Text="Add" Command="{Binding AddCommand}"></Button>

            <Button Text="Back" Command="{Binding BackCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>